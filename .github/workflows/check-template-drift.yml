name: Template Drift

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  merge_group:

permissions: {}

jobs:
  drift:
    uses: netresearch/.github/.github/workflows/check-template-drift.yml@main
    with:
      template: typo3-extension
    permissions:
      contents: read
