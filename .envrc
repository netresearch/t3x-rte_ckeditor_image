# direnv configuration for rte_ckeditor_image
# Auto-generated by agent - Last updated: 2025-10-15
#
# Usage: Run `direnv allow` to activate this configuration
# Install direnv: https://direnv.net/

# Add composer bin directory to PATH
PATH_add vendor/bin

# PHP development environment
export PHP_CS_FIXER_IGNORE_ENV=1

# Composer configuration
export COMPOSER_PROCESS_TIMEOUT=600

# Load local environment overrides if present
dotenv_if_exists .env.local

# Display activation message
echo "✅ rte_ckeditor_image development environment activated"
echo "📦 Composer bin tools available: php-cs-fixer, phpstan, phplint, rector"
echo "🔧 Quick commands: make help"
